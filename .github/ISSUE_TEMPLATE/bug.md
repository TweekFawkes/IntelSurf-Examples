---
name: Bug report
about: A recipe or guide isn't doing what it says.
title: "[bug] "
labels: bug
---

**What recipe / file?**
e.g. `recipes/scans/surf_one_shot.py`

**What did you run?**
The exact command, with any env vars set (redact your API key).

```bash
export INTELSURF_HOST=https://dev.intel.surf
export INTELSURF_API_KEY=sk-slimestack-XXXX  # redacted
python recipes/scans/surf_one_shot.py example.com
```

**What happened?**
Paste the output, error, or unexpected behavior.

**What did you expect?**
One sentence is fine.

**Environment**
- OS:
- Python:
- IntelSurf CLI version (`intelsurf version`):
- Host (`https://dev.intel.surf` / stage / prod):
